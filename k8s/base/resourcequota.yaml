apiVersion: v1
kind: ResourceQuota
metadata:
  name: jest-api-quota
  labels:
    app.kubernetes.io/name: jest-api
    finops.ohanyere.io/cost-center: payments
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi
    pods: "10"
