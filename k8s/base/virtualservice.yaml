apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: jest-api
  labels:
    app.kubernetes.io/name: jest-api
spec:
  hosts:
    - jest-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: jest-api
            port:
              number: 80
          weight: 100
